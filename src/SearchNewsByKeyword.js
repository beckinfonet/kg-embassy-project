import React from 'react';
import App from './App';


const SearchNewsByKeyword = props => (
        <div>
            <a href={props.url} >{props.title} </a>
        </div>
)


export default SearchNewsByKeyword;